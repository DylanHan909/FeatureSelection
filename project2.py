import numpy as np
import copy

def main():
    print("Welcome to Dylan's Feature Selection Algorithm.")
    filename = input('Type in the name of the file to test: ')

    algorithm_type = input('Ty[pe the number of the algorithm you want to run.')
main()